let defaultResolver;

module.exports = () => {
  if (!defaultResolver) {
    try {
      defaultResolver = require(`jest-resolve/build/defaultResolver`).default;
    } catch (error) {
      defaultResolver = require(`jest-pnp-resolver/node_modules/jest-resolve/build/default_resolver`).default;
    }
  }

  return defaultResolver;
};
